"""Configuration package for ESP32-B.

Imported by: All modules requiring configuration
Provides: config (main config), wifi_config (WiFi credentials)

Configuration modules:
- config: Main configuration from config.json (pins, intervals, actuator settings)
- wifi_config: WiFi SSID and password for logging and OTA

All configuration is loaded at import time and exposed as module constants.
"""
# Configuration module
